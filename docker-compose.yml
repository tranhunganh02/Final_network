version: '3'

services:
  backend:
    build: ./Backend
    ports:
      - "3002:3002"
    depends_on:
      - database
    env_file:
      - ./Backend/.env
    environment:
      DB_HOST: localhost
      DB_USER: root
      DB_PASSWORD: Tha@021103
      DB_DATABASE: docker

  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:80"
  #   depends_on:
  #     - backend

  database:
    build: ./database
    ports:
      - "3306:3306"
    volumes:
      - database-data:/var/lib/mysql

volumes:
  database-data:
